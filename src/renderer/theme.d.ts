import '@mui/material';
declare module '@mui/material/styles' {
  interface Palette {
    table: {
      background: string;
      secondary: string;
    };
  }
  interface PaletteOptions {
    table: {
      background: string;
      secondary: string;
    };
  }
}
